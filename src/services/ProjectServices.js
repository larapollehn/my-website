const dataAccess = require('../data/DataAccess');

const checkCache = (request, response) => {

}

const getProjects = (request, response) => {

}

module.exports = {
    checkCache,
    getProjects
}